# macOS
.DS_Store

# Per-machine Claude Code state (permission allowlists, session data)
.claude/

# Secrets — defence in depth, in case anything ever lands here
*.env
*.key
*.pem
secrets/
credentials/

# Local-only WebBank scratch (spec §Architecture: not used by routines)
staging/
